{
  "name": "seijaku-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "sh -c 'if [ -x backend/run-backend.local.sh ]; then cd backend && ./run-backend.local.sh; else npm run dev --workspace backend; fi'",
    "build:frontend": "npm run build --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "build": "npm run build:frontend && npm run build:backend"
  }
}
