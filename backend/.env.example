PORT=4001
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/seijaku_backend?schema=public"
JWT_SECRET="change-me"
ADMIN_EMAIL="admin@seijaku.local"
ADMIN_PASSWORD="password123"

# Storage driver: "local" (default, dev only), "s3", or "vercel-blob"
# STORAGE_DRIVER="local"
# BLOB_READ_WRITE_TOKEN="" # required when STORAGE_DRIVER=vercel-blob (auto-injected by Vercel Blob integration)
